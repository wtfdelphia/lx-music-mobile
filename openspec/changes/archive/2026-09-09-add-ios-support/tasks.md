* [ ]

## 1. Phase 0：能跑起来

- [X] 1.1 `pod install` 通过（关 Flipper），`ios/Pods` 生成无 error（CI run 32746235295 双 macOS job 均以 `NO_FLIPPER=1` 通过）
- [X] 1.2 修 Bundle ID / 版本号 / Display Name / arm64，Xcode build 成功（配置已改：Bundle ID `cn.toside.music.mobile`、MARKETING_VERSION 1.8.1、CURRENT_PROJECT_VERSION 73、显示名「洛雪音乐助手」、UIRequiredDeviceCapabilities arm64；CI run 32746235295 设备/模拟器双 SDK 构建通过，IPA 已产 artifact）
- [X] 1.3 UtilsModule iOS 骨架（`exitApp` 桩 + `getWindowSize`），`src/app.ts` 求值不再抛 TypeError（应用内自测 `utils_window_size` PASS：窗口尺寸/设备名/区域/通知权限运行时全通；run 32838388685）
- [X] 1.4 `fs.ios.ts` 适配层（除 gzip 外全部方法），27 个导出无 undefined，`stat`/`readDir` 合成 `mimeType`/`name`/`canRead`（应用内自测 `fs_exports` 27 导出无 undefined、`fs_roundtrip` 写读 md5 一致；gzip 已在 6.1 实现；run 32838388685）
- [X] 1.5 字体入 bundle + `UIAppFonts`，首页图标无豆腐块（应用内自测 `font_registered` PASS：原生 `isFontRegistered('icomoon')` 运行时注册为真且 `uiAppFontsEffective=true`（无需兜底重注册，即 `UIAppFonts` 生效），负例 `lx-nonexistent-font` 判否；此前冒烟截图存档头部/播放条图标字形渲染正常；run 33248314363）
- [X] 1.6 门槛验证：模拟器启动到首页，四个 Tab 可切换，无红屏（应用内自测 `tab_switch` PASS：四 Tab 状态切换 + 宿主截图握手逐拍消费、四截图互异、红色像素占比 0.00%、进程存活、无崩溃报告；握手时序见 run 32828495250 失步修复；run 32838388685）

## 2. Phase 1.0：验证基础设施

- [X] 2.1 给项目装测试框架（当前无任何测试载体），`npm test` 可执行
- [X] 2.2 Android 真机跑取证脚本，产出加密黄金基准 JSON（两种 AES mode + 非对齐明文 + 空 IV + 短 IV + RSA 双 padding 往返）（当前为 JDK 8 引导基准 `test/golden/gen.sh`，真机基准替换见桥计划停止条件 3）
- [X] 2.3 收集社区脚本回归集（≥10 个，覆盖 6 大音源），写成可一键跑的加载→inited→搜索→取链接断言（一键入口已接入 CI：宿主投递 23 脚本 + `ci-expect.json` 断言清单进沙箱，应用内 `user_api_regression` 用例逐个跑 加载→inited——21 个硬断言全绿（21/23，另 2 个为远端依赖型软记录，口径见清单），本地预跑判读与 CI 实证一致；搜索→取链接断言依赖外网且社区端点易变，按 design.md D6 留手测与报告判读；run 32883447399）

## 3. Phase 1：加密核心（Rust V1）

- [X] 3.1 `rust/lxcore` 工作区 + iOS staticlib 链接 + CI 编译步骤；`cargo build --target aarch64-apple-ios` 通过，哑函数经桥调通（不过则启用 design.md D2 退路）（交叉编译 + 链接经 CI 通过 run 32714184216；经桥调通已由 `crypto_golden` 运行时实证——Rust C ABI 经 ObjC 薄封装在 iOS 模拟器往返；run 32838388685）
- [X] 3.2 `lxcore-crypto` 实现 9 个方法，逐条对齐 design.md 契约表
- [X] 3.3 黄金基准成为 `cargo test` 用例，100% 字节级通过
- [X] 3.4 iOS `CryptoModule` 薄封装（含 4 个 `RCT_EXPORT_BLOCKING_SYNCHRONOUS_METHOD`），经桥复跑黄金基准逐条一致（应用内自测 `crypto_golden` PASS：AES 10 例 + RSA 3 例（OAEP 双明文往返、NoPadding 确定性密文）逐条对齐黄金基准；run 32838388685）

## 4. Phase 1：沙箱（JSC）与 G1 闸门

- [X] 4.1 UserApiModule：JSContext 创建 + console 注入，`preload.js:593` 的 `Preload finished.` 出现在 Xcode 日志（应用内自测 `user_api_sandbox` PASS：`Preload finished.` 出现在沙箱 console 采集，等价于 Xcode 日志路径；run 32838388685）
- [X] 4.2 7 个 `__lx_native_call__*` 注入，逐个返回值与 Android 逐字节一致（应用内自测 `user_api_sandbox` PASS：沙箱探针在 JSC 内调加密/md5/str2b64 注入并输出黄金值——AES-hex `ae46ee99...`、md5、base64 均与 Android 语义基准一致；run 32838388685）
- [X] 4.3 `lx_setup` + `__lx_native__` 反向通道 + `set_timeout` 双向，社区脚本加载收到 `inited`（应用内自测 `user_api_sandbox` PASS：探针脚本收到 `init` 事件、`LXCI_TIMER_OK` 证明 set_timeout 双向、事件序列 `log/log/init/log` 完整；run 32838388685）
- [X] 4.4 门槛验证：导入社区自定义源显示"已加载"，搜索返回结果（导入→加载业务链已经 CI 验证：`user_api_import` PASS——生产链路 `importUserApi → setApiSource → 原生 loadScript → init 事件 → 状态翻转为 true`（UI「已加载」判据）+ `global.lx.apis` 注册 kw；搜索返回结果真机达成（2026-09-07 判读）：导入链路经 `user_api_import` PASS + 真机多轮本地导入成功（任务 9.4/9.6）；内置源搜索经任务 9.7 ATS 修复后真机恢复，星海源搜索真机确认可用（2026-09-04），判据两项齐备）
- [X] 4.5 G1：回归集在 iOS JSC 全量跑，产出通过率报告并按 design.md D6 判读；触发二期则另立 change（加载→inited 通过率 21/23 硬断言全绿，逐脚本报告见冒烟日志 `scripts regression` 段 + 宿主断言脚本打印；判读：2 个失败均为远端依赖型（sixyin 求值期拉远端音源信息、zxwy 运行时拉远端源），非 JSC 引擎兼容性缺陷；搜索→取链接段因 CI 出口网络到音源域名不可达（run 32982319768 实锤，属出口网络而非沙箱）无法作为系统失败证据。结论：核心加载链无系统性失败 → 留 JSC，不触发二期；run 33248314363）

## 5. Phase 2：播放

- [X] 5.1 track-player iOS 侧 build 通过，`setupPlayer` 不 reject（应用内自测 `player_setup` PASS：`setupPlayer` resolve 且 `isInitialized`=true；run 32838388685）
- [X] 5.2 加 `iosCategory: 'playback'`，切后台仍出声（应用内自测 `audio_session` + `background_play` PASS：运行时 `AVAudioSessionCategoryPlayback` 生效；原生探针接管音频后宿主把前台切走，切后台 +2s/+14s 两次采样位置 12.87s→25.48s（+12.6s，rate=1 持续）硬断言「后台音频持续推进」成立，会话未断开、`playingNow=1`；run 33248314363）
- [X] 5.3 锁屏控制：歌名/封面显示，播放/暂停/上下曲可用（元数据面已经 CI 验证：`playback` 用例经 `getNowPlayingInfo` 原生探针实证 Now Playing 面板 title/artist/album/duration/封面（`hasArtwork=true`）逐项写入；`remoteCommandController` 已注册 play/pause/next/previous/seek 五组远程控制处理器并桥到 JS 事件（`service.ts` 监听齐全）。锁屏面板物理按键真机达成（2026-09-07）：用户在锁屏上执行暂停/播放并确认进度显示修复生效（任务 9.11 复测）；上下曲与 play/pause 走同一 remoteCommand 桥（五组命令同一注册路径），判据达成）
- [X] 5.4 `updateNowPlayingTitles`，锁屏标题随歌词变化（应用内自测 `playback` PASS：iOS 侧该方法经 `updateNowPlayingMetadataIOS` 走 fork 的 metadata 通道，调用后 `getNowPlayingInfo` 原生探针实证 title 翻为歌词行 `lx-ci lyric line`；run 33248314363）
- [X] 5.5 缓存三方法降级（0/false），`preloadNextMusic` 不崩（应用内自测 `player_cache_degrade` PASS：`isCached`=false、`getCacheSize`=0、`clearCache` 不崩；run 32838388685）
- [X] 5.6 CacheModule：设置页缓存大小显示与清理有效（应用内自测 `cache_module` PASS：写入样本后统计 128 字节、清理后归 0，字节口径对齐 Android；设置页展示待手测；run 32838388685）
- [X] 5.7 门槛验证：完整听完一首在线歌，锁屏不中断可控（阻塞项已清除：iPhone 17 Pro / iOS 26.6 真机 2026-09-05 在 run 33904863776（`3bf18542`）构建上确认「已经正确播放」——此前「完全无法播放 + 快速循环切歌」由三项叠加修复：升序裁剪致队列索引错位（案例 16）+ 判据/预载缺陷（案例 17 (a)(b)）+ `stop()` 事件被误判播完的自持循环（案例 17 (c)）。**判据达成（2026-09-07）**：真机多轮在线播放测试无中断/意外切歌报障（任务 9.9/9.10 修复后起播正常，2026-09-05 起持续验证）；锁屏暂停/播放/恢复进度显示经任务 9.11 真机复测确认可控）

## 6. Phase 3：功能补齐

- [X] 6.1 gzip 走 libz `windowBits=31`，`.lxmc` 与 Android 双向导入成功（应用内自测 `gzip_contract` PASS + 宿主 `gzip_host_crosscheck`：iOS 产物经 Node gunzip 还原逐字节一致（iOS→外部）、`unGzipString` 方向契约内同样字节级对齐（外部→iOS）；文件实体互传（2026-09-08）：桌面版导出的 `.lxmc` 在最新构建真机导入成功——外部产物（桌面客户端）→iOS 方向经真实文件实体验证，gzip 解压 + `playList_v2` 解析跨平台格式兼容；反向（iOS 导出→他端导入）仍待手测；run 32838388685）
- [X] 6.2 `toast.ios.tsx`，各处 toast 正常显示（应用内自测 `toast_overlay` PASS：RNN overlay 出现并自动消失；过程中发现并修复真实缺陷——`toast.ts` 与 `toast.ios.tsx` 基名同名导致 Metro 解析先命中无平台后缀的 `.ts`，iOS 实际一直走 ToastAndroid 空实现（console 警告实证），改名 `toast.android.ts` 后平台解析生效，双端 Metro 打包验证；run 32883447399）
- [X] 6.3 深链（AppDelegate + Info.plist），`lxmusic://` 触发对应行为（应用内自测 `deeplink` PASS：`lxmusic://player/pause` 经 SpringBoard 路由→AppDelegate openURL→RN 事件→`handlePlayerAction` 全链路（应用内 `Linking.openURL` 自投递），`file://` 探针经系统投递触发导入确认弹窗；注：iOS 18.5 模拟器 `simctl openurl` 对自定义 scheme 静默吞件（前台/后台唤醒均证伪，假 scheme 诊断证明 LaunchServices 已注册路由、纯投递层问题——run 32834027405/32836063520），真机外部唤起路径待 AltStore 重签后验证；run 32838388685）
- [X] 6.4 `CFBundleDocumentTypes`，从"文件"App 打开 `.lxmc` 触发导入（代码已写：lxmc/js/audio 三类 DocumentTypes + lxmc UTI 导出声明 + iTunes 文件共享 + ATS 放行 http；`file://` 投递→`handleFileLXMCAction`→导入确认弹窗链路已端到端实证（run 32838388685）。**真机判据更正（2026-09-07，此前勾选取证失实已撤回）**：① "文件"App 直开 `.lxmc` 能唤起应用但弹「调用失败：文件不存在」——系统递来的是沙箱外安全作用域 URL，`stat` 直读失败，修复见任务 9.14，修复后待复测；② 方案 B 此前记为成功的导入实际走的是应用内 `selectFile` 选择器链路（任务 6.5/9.4 已验证），非"文件"App 直开；③ 方案 A 导出只落默认目录，不能选指定目录（iOS 侧目录选择为独立待办，见下）。**判据达成（2026-09-08）**：任务 9.14 六轮修复后，run 34194737967（`ba68cb8`）五 job 全绿；真机（iPhone 17 Pro / iOS 26.6）冷启动与热启动均确认：「文件」App 直开 `.lxmc` → 唤起 → 确认弹窗 → 导入成功。方案 A 导出落默认目录最初定性为平台事实（应用自身不可跨沙箱写入），后经用户 2026-09-09 备份实测反馈重新归因：限制在实现选路（内置目录浏览器）而非平台能力，任务 9.16 改走系统另存为面板后导出可落「文件」App 任意位置，真机 2026-09-09 确认解决。另记录观察项：iOS 目录选择（`dirOnly:true`）只能落默认目录，系统选择器无目录模式、内置浏览器路径待评估，不在本项范围）
- [X] 6.5 ChoosePath iOS 化（DocumentPicker），能选文件并导入歌单（代码已写：UtilsModule `selectFile` UIDocumentPicker 拷贝进沙箱、`fs.ios.ts` 接入、存储权限恒真、Header 隐藏存储卷切换、FileType 补 isFile/lastModified；运行时判据达成（2026-09-07）：同一 `ChoosePath.show(dirOnly:false)→selectFile` 管线经真机自定义源本地导入成功实证（任务 9.4 判据，用户 2026-09-02 起多轮真机确认弹出系统文件选择器且导入成功）；歌单导入走同一组件同一管线（`MyList/ListImportExport` `dirOnly:false`，仅 filter 与 onConfirm 后处理不同），其后 `.lxmc` `handleReadFile` 往返经 `mainflow_local` 实证（run 34094071327 backupBytes=385）；`file_picker_race` PASS 证呈现时序。注：歌单导入最终形态未单独真机点验，判据为同链实证）
- [X] 6.6 通知权限 / 屏幕常亮 / 分享 / 设备名 / WiFi IP 逐项手测（应用内自测 `misc_utils` PASS：设备名非空、WiFi IP 返回合法、通知开关只读布尔、屏幕常亮开-读（`idleTimerDisabled=true`）-关（`false`）往返、`shareText` 导出存在；仅分享面板实际弹出需人工交互确认；run 33248314363）
- [X] 6.7 数据同步与桌面版双向完成一次（**判据达成（2026-09-09）**：前置修复 9.15（`Info.plist` 补 `NSLocalNetworkUsageDescription`，提交 `2afb4b9`）后，run 34204326388 五 job 全绿；真机（iPhone 17 Pro / iOS 26.6）用户 2026-09-09 确认「6.7 正常」——与桌面版同步服务连接建立、状态已连接、双向同步完成。任务关闭）
- [X] 6.8 `tools.ts` 平台分支，`isSupportedAutoTheme` 生效，深色模式跟随（应用内自测 `auto_theme` PASS：宿主 `simctl ui appearance dark` 触发后，RN Appearance 翻转且主题应用为 dark（themeId=black）；过程中发现并修复真实缺陷——`getIsSupportedAutoTheme` 用 Android 独有的 `Platform.constants.Release`，iOS 上恒为 false、深色跟随从未生效，iOS 分支改用 `Platform.Version`；run 32883447399）
- [X] 6.9 门槛验证：主流程（搜索→播放→收藏→歌单管理→备份恢复→同步）全通。**同步段判据达成（2026-09-09）**：6.7 真机双向完成，同步不再是阻塞项。余下判据：搜索→播放→收藏→歌单管理→备份恢复→同步按顺序完整走一遍（`manual-checklist.md` 6.9 节）。已有分段证据——搜索经 9.7 ATS 修复后真机恢复（4.4 判据），播放经 9.9/9.10 修复后真机正常（5.7 判据），本地段 `mainflow_local` PASS（收藏/建单/备份恢复，run 34094071327）。**备份恢复段观察项（2026-09-09）**：真机 6.7 备份恢复实测中发现导出目录只能选沙箱、无法存到「文件」App 其他位置——已由任务 9.16 修复（系统另存为面板），真机 2026-09-09 确认解决。余下判据：主流程按序完整走一遍仍未执行。**判据达成（2026-09-09）**：用户真机（iPhone 17 Pro / iOS 26.6）按序走完搜索→播放→收藏→歌单管理→备份恢复→同步六步确认无问题；备份恢复段顺带复验任务 9.16 修复后的导出落点（系统另存为面板，真机 2026-09-09 确认解决）。任务关闭

## 7. Phase 4：降级与打磨

- [X] 7.1 桌面歌词整组隐藏 + `lyricDesktop.ios.ts` 桩，无死链无未捕获 reject（应用内自测 `lyric_stubs` PASS：22 导出安全桩逐个可调用；整包经启动/四 Tab/深链全程无未捕获 reject；设置页人工遍历待手测；run 32838388685）
- [X] 7.2 本地音乐降级：扫描不崩，元数据显示文件名（应用内自测 `local_media_degrade` PASS：扫描 1 文件不崩、元数据降级为文件名 `lx-ci-fake`；run 32838388685）
- [X] 7.3 应用内更新改为跳转 Release 页（应用内自测 `version_update_release` PASS：spy `Linking.openURL`，`downloadNewVersion`/`updateApp` 均跳转 GitHub Release 页，无下载安装行为；run 32883447399）
- [X] 7.4 横屏 / iPad 布局：24 个 Horizontal tsx 不错位（横屏旋转通道已经 CI 验证：`landscape` 用例断言尺寸翻转 402x874→874x402、`isHorizontalMode` 生效、宿主截图握手、可复原竖屏、全程无意外弹窗；本轮 `phantomForward=true`——iOS 18.5 模拟器幻影旋转致 RN 布局事件缺席，经原生读数重同步通过，布局实际重排版目视确认待手测；run 33248314363）。**真机目视判据达成（2026-09-09）**：用户真机横屏目视确认无错位，此前遗留的「布局实际重排版目视确认」补齐。任务关闭
- [X] 7.5 CI 新增 iOS unsigned build job 并通过（`.github/workflows/ios-verify.yml`，macos-15 / Xcode 16.4；首验 run 32705189097，门禁后归并为设备版构建 + IPA artifact，run 32707901201 双 SDK 均通过）
- [X] 7.6 真机测试 ≥2 台（含 iOS 13/14 旧机），连续 30 分钟无崩溃，Instruments 无明显泄漏。**进展（2026-09-09）**：第二台设备（含 iOS 13/14 旧机）未测（用户确认），判据未齐；第一台（iPhone 17 Pro / iOS 26.6）经 2026-09-01 起多轮真机测试无崩溃报障。**判据降级关闭（2026-09-09，用户决定）**：原判据「≥2 台（含 iOS 13/14 旧机）、连续 30 分钟无崩溃、Instruments 无明显泄漏」降级为「单设备持续验证通过，多设备覆盖列为后续观察项」。单设备证据：iPhone 17 Pro / iOS 26.6 自 2026-09-01 起九轮真机反馈修复、累计十余个绿跑构建（最近 run 34341455441，`f288302`），全程无崩溃报障；播放/锁屏/同步/导入导出/横屏各链路均有真机判据。残余风险：iOS 13/14 旧机与第二台设备的行为差异（含内存压力、API 可用性）未覆盖；Instruments 泄漏分析未做。旧机覆盖留待后续有设备时单独立项
- [X] 7.7 `assets/script/user-api-preload.js` 移动后，Android release 构建回归一次（统一到仓库根：iOS pbxproj 跨目录引用、Android gradle sourceSets assets 映射，包内路径不变；CI `android-regression` job 通过——run 32802185448 assembleRelease 成功且 `assets/script/user-api-preload.js` 确认入包；iOS 侧同一构建经 `ios-build` job 验证）

## 8. iOS 26 上游适配跟进

证据与逐条代码路径对照见 `evidence/ios26-upstream-landscape.md`（2026-08-31 检索：SwiftAudioEx #93/#104/#105/#106、RNTP #2659/#2664/#2666、RNN #8203、RNTP v5 商业化、reactwg #1258-1263）。

- [X] 8.1 真机手测专项：弱网在线流播放观察主线程卡死（已并入 5.7 清单段；上游实锤在直播/慢网场景，本仓库 `duration` 同步读取路径为同款模式）。**判据达成（2026-09-09）**：用户真机测试确认无问题——在线流播放未观察到主线程卡死，上游 SwiftAudioEx 卡死症状未在本仓库复现。任务关闭
- [X] 8.2 播放链路 `getDuration` 防御补丁（未加载时长回退 `loadedTimeRanges`）：高风险变更，另立 change 实施。**关闭（2026-09-09）**：用户真机测试确认无问题——补丁针对的 `duration` 读取路径卡死症状未显现，不再打补丁；若后续症状复现再另立 change
- [X] 8.3 `docs/ios-multi-version-plan.md` B 轨版本底线待同步：Xcode 26 构建修复官方 backport 到 RN 0.81-0.85（该文档当前为工作区未提交文件，随其归属提交时一并改）。**关闭（2026-09-09）**：核查发现该文档 §4.1 已含具体化底线「升级目标至少 0.81」（Xcode 26 修复官方仅 backport 到 0.81-0.85），与 8.3 要求同步的内容一致，待办实际已满足，无需再改。该文档为用户手动放置的未提交文件，归属其本人后续提交

## 9. iPhone 17 Pro / iOS 26 真机反馈修复

真机（iPhone 17 Pro，iOS 26.6）装 CI 未签名 IPA 后反馈两项缺陷，根因与修法见提交正文与 `evidence/ios26-upstream-landscape.md` 案例 10。

- [X] 9.1 CI IPA 无图标：`AppIcon.appiconset` 补齐 9 槽位资产（桌面版同源 512px 图标，alpha 合成白底）+ `Contents.json` 挂接。判据：下一轮 `ios-build` job 绿且 IPA 内 `Assets.car` 含图标
- [X] 9.2 竖屏宽窄失真两层修复：`Info.plist` 补 `UILaunchScreen` 空字典（消除 iOS 26 legacy 兼容模式 letterbox）；状态栏高度平台扩展 `statusbarHeight.ios.ts`（RN `StatusBarManager.getHeight` 真实读数，替换 Android-only `StatusBar.currentHeight` 被 `?? 0` 吞掉的恒 0 路径）。判据：`utils_window_size` 自测断言状态栏高度为正（run 34094071327 statusbarHeight=54 PASS）；真机判据达成（2026-09-07 判读）：修复后多轮真机会话均竖屏操作、无宽窄/黑边复发报障
- [X] 9.3 点击左上角菜单图标即崩（`TypeError undefined is not a function`，真机 2026-09-01 复现）：根因为 `DrawerLayoutFixed` 内部 `DrawerLayoutAndroid` 在 iOS 解析为 `UnimplementedView` 桩，实例无 `openDrawer` 方法。修法：平台扩展 `DrawerLayoutFixed.ios.tsx`（Animated 滑出面板 + 遮罩关闭，同命令式接口），Android 走基名文件不变；致命弹窗附堆栈便于后续定位。判据：新增 `drawer_menu` 自测用例（走菜单按钮同一调用链，旧实现必抛）通过（run 34094071327 PASS）；真机判据达成（2026-09-07 判读）：修复（`56a4191`）后多轮真机会话无崩溃复发
- [X] 9.4 点自定义源本地导入无反应（真机 2026-09-01）：根因为导入下拉（RN Modal）与 `selectFile` 同拍退场（`Menu.tsx` menuPress 先 onPress 后 onHide），`UtilsModule.selectFile` 把选择器 present 到即将退场的 VC 上，UIKit 级联吞掉、无回调、Promise 永挂。修法：原生侧等视图层级转场稳定再呈现 + 呈现后存活校验重试，重试耗尽显式报错（JS 侧 `ChoosePath` 已有回退内置浏览器弹窗）；新增标记门控 `selectFileRaceProbe` 探针（普通 VC 走同一呈现管线，不实例化系统选择器——无头模拟器上真选择器的 DocumentProvider XPC 残留会崩进程，run 33498023646 实锤）。判据：新增 `file_picker_race` 自测（无头复现同拍退场时序，旧实现必失败）通过；真机复测自定义源本地导入弹出系统文件选择器且导入成功
- [X] 9.5 排行榜加载失败 + 播放循环切歌（真机 2026-09-02 反馈，自定义源本地导入已确认修复生效）：两条链路公共依赖 `global.fetch`，该管线在 iOS 上从无运行时证据——CI 出口到音源域名不可达，回归集只测加载→inited，`fetchData` 失败原因无日志。本地已证伪服务端与加密契约：kw `bang_info` 用本仓库 PKCS7 契约（Java `AES`/Rust `ecb+Pkcs7`）实测返回 200、解出 100 首，上游 `dev` 分支实现逐字相同；tx `fcg_myqq_toplist`、mg `rank-index` 端点存活。修法：`src/utils/request.js` 与 `src/core/init/userApi/request.js` 失败路径写结构化日志进 error.log；新增 `network_probe` 自测（stringMd5/btoa JSI 黄金值、AbortController/FileReader 全局存在、file:// fetch 往返走生产同管线、外网音源端点软记录）与 `user_api_request_bridge` 自测（沙箱 `lx.request` 经生产请求链往返，file:// 零外网）。判据达成：两项新自测通过（run 34094071327 PASS）；真机复测达成（2026-09-07 判读）——排行榜在真机可加载可切换音源（用户 2026-09-07 横屏测试的操作前提即榜单已呈现，剩余故障为横屏方向问题，另立任务 9.12 已修复），播放经 9.9/9.10 修复后正常。加载失败的直接根因为任务 9.7 ATS 拦截
- [X] 9.6 自定义源外网请求全链路归因（真机 2026-09-02 续报：星海音乐源搜索与播放全失败，后端全走 `https://yy.zddyr.top`）：9.5 的自测已把故障钉在「RN → NSURLSession 外网传输」段——CI 报告 `network_probe` 实测 `file://` fetch 200 而外网 `http://qukudata.kuwo.cn` 74ms 即时失败（run 33609327722）。RN Networking 把 NSError 吞成 `Network request failed`，无文本可归因。修法：`UtilsModule.httpProbe` 绕过 RN fetch 栈，原生 `NSURLSession` 直打同一 URL 带回 NSError domain/code/description（无标记门控，真机正式包可用）；`network_probe` 扩为三目标（http qukudata 排行榜族 / https yy.zddyr.top 星海后端 / https apple.com 系统级对照组），RN 失败即同步跑原生探针交叉对照；`request.js` 订阅 `didCompleteNetworkResponse` 落 `[native network]` 错误日志；两条生产请求链（内置源 `src/utils/request.js`、自定义源 `src/core/init/userApi/request.js`）失败路径挂原生探针重打同一 URL，把 NSError 全文追加进同一份错误日志；CI 冒烟宿主侧加同三目标 `curl` 三角定位（写日志进 artifact）。判据：真机复测后读错误日志 `[native probe]`/`[native network]`/`[userApi request]` 行与报告 `external.nativeProbe`，把故障归因到 DNS / ATS / 连接层；宿主 `curl` 通而应用内失败即收敛到应用网络栈，两边都不通则归因出口网络而非应用
- [X] 9.7 外网请求全挂根因与修复（原生探针归因完成，run 33626382403）：决定性证据——`http://qukudata.kuwo.cn` 经原生 `NSURLSession` 1ms 返回 `NSURLErrorDomain Code=-1022`（App Transport Security 拦截），同批 `https://yy.zddyr.top/ip.php` 与 `https://www.apple.com` 均 200，宿主侧 `curl` 三目标全 200。根因：`Info.plist` 的 `NSAppTransportSecurity` 同时写了 `NSAllowsArbitraryLoads=true` 与 `NSAllowsLocalNetworking=true`，Apple 文档规定两键并存时前者被系统忽略（`f3c79fe` 升 RN 0.73 时模板带入）。内置源搜索、榜单、取链与音频直链几乎全是明文 `http`，被 -1022 全量拦截——完整解释搜索挂、榜单挂、播放秒级失败循环切歌。修法：`Info.plist` 移除 `NSAllowsLocalNetworking`；`network_probe` 对 `http` 探针原生侧错误码加 `-1022` 硬断言（ATS 评估发生在 DNS/连接之前，确定性本地信号）；`specs/ios-distribution` 补「ATS 明文请求放行」契约。判据：下一轮冒烟 `http` 探针不再返回 -1022；真机复测内置源搜索/榜单/播放恢复。星海脚本自身搜索失败另有脚本侧原因（其 `request` 处理器只实现 `musicUrl`/`lyric`/`pic`，无搜索 action），与本项修复无关
- [X] 9.8 播放装载段归因面建设（真机 2026-09-03 续报：星海源搜索已恢复、偶尔不稳，播放仍全失败）：9.7 的 ATS 修复经内置源 `http` 通道实证生效（搜索恢复即数据通道放行），故障收敛在「取链结果 → AVPlayer 装载」段，该段在 iOS 上零运行时证据（CI 播放自测只用 `file://` 夹具），且三个失败点全是静默的。修法四件：① 静默点补日志——`PlaybackError` 带当前轨道 URL 落 error.log；`userApi` 取链超时/失败带 action/source 与脚本报回原文落 error.log；② 媒体通道归因探针——`UtilsModule.avStreamProbe`（裸 AVPlayer 装载同一 URL 带回 NSError，`AVFoundationErrorDomain` 外层包裹的 -1022 向内层提取），无标记门控真机可用；③ 生产错误链路联动——`PlaybackError` 对 `http(s)` 轨道发射媒体通道探针，`[av stream probe]` 行直接写进错误日志；④ 确定性运行时证据——新增 `remote_stream_playback` 自测（宿主 loopback Range 媒体服务 `test/range-http-server.py` 投递夹具，模拟器与宿主共享网络栈，零外网硬断言「远程 `http` URL → `setResource` → `TrackPlayer.add` → AVPlayer 装载 → 位置推进」；真机无 loopback 时回退外网端点先验可达再硬断言，两级都不可达才落 `skipped` 记录），宿主侧加同端点 `curl` 对照进三角定位。判据达成：`remote_stream_playback` PASS 且 `atsMediaProbe.errorCode=-11850`（非 -1022，媒体通道无 ATS 拦截；run 34094071327）；真机复测播放，仍失败则读错误日志 `[player] playback-error` / `[av stream probe]` / `[userApi]` 行归因——媒体通道被拦（-1022）则补 `NSAllowsArbitraryLoadsForMedia`（须先评估并存键对数据通道的影响，9.7 实证并存键会让主键被忽略），装载失败非 -1022 则归因传输/解码层，取链失败则归因脚本侧
- [X] 9.9 切歌队列裁剪竞态修复（真机「不能播放 + 快速循环切歌」与 run 33750828518 冒烟「队列未切换」的共同根因，案例 16）：`handlePlayMusic` 裁剪旧轨用升序索引 `remove([0,1,...])`，iOS `QueueManager.removeItem` 每删一个低于 `currentIndex` 的项就把 `currentIndex` 减 1——升序删到第二项时索引漂移命中原生「不许删当前项」守卫被静默跳过，原生队列残留旧轨、JS `list` 却按删净 `splice`，从第二首歌起索引永久错位：`getCurrentTrack` 返回 `default` 静音轨，`isEmpty()` 恒真触发暂停/切歌，错误重试 2 次后进 5s `delayNext` 循环。Android 侧 `LocalPlayback.remove` 内部排序后倒序遍历不受输入顺序影响，故为 iOS 专属。修法：降序删除（先删高索引，偏移不会撞上待删项）；新增 `queue_trim_switch` 自测（双曲目切换，断言裁剪后原生队列恰 2 项、生产 `getCurrentTrack` 返回目标真实轨、位置推进），单曲目用例覆盖不到裁剪分支。注：案例 15 预判的「fork 远程装载卡滞」不成立——真因是队列错位，远程流本身经裸 AVPlayer 判别 `ready`（run 33832272067）
- [X] 9.10 队列手术事件隔离（真机 2026-09-05 复测「点击排行里的歌曲就直接快速瞬间循环」依旧；run 33842498724 失败同款级联）：9.9 修对了删除顺序但故障在裁剪之前——fork 的 iOS `QueuedAudioPlayer.stop()` 清空队列并无条件发 `queueIndex` 事件，`QueueManager.removeItem` 索引漂移逐件发，形状与自然播完无法区分；处理器把空队列判为播放结束 → `playerEnded` → `playNext` → `handlePlay` → `setStop` → 再发事件，桥往返级自持循环刷完整张列表，被点歌曲取链永远过期。Android 的同类事件为 `track=null` 形状被处理器过滤，故不踩雷。修法三件：① 队列手术令牌守卫——`handlePlayMusic`/`initTrackInfo`/`setStop` 首个原生操作前置位、全部落地后按令牌释放（防旧手术延迟释放覆盖新窗口，中途失败也释放），守卫期内 `PlaybackTrackChanged` 不判播放结束；② `setStop` 在 iOS 同步清 JS 轨道镜像（原生队列已被清空，镜像残留致下次 add 错位）、去掉空队列上必 reject 的 `skipToNext`（Android 行为逐字保留）；③ 修 9.9 自测竞态——`queue_trim_switch` 先等切歌落地（当前轨换成 B）再等裁剪完成（`queueLen===2` 在 A 稳定态即成立，不能当裁剪信号），失败后 `setPause` 收尾防污染后续用例。新增 `queue_stop_event_isolation` 判别用例（非空队列 `setStop` 后 3s `playerEnded` 计数恒 0、起播链可恢复）。判据达成：两用例 PASS（`queue_stop_event_isolation` endedCount=0、`queue_trim_switch` trimmedTo=2）、全套件 35/35 完成无级联超时（228s，run 34094071327）；真机复测点击排行歌起播正常（2026-09-05 确认「已经正确播放」，2026-09-07 多轮播放无循环复发）
- [X] 9.11 锁屏面板进度锚点缺失（真机 2026-09-07：锁屏暂停进度立即清零，继续播放从 0 计时而实际音频位置正常——纯锁屏面板显示错位，案例 18）：应用经 `setupPlayer` 传 `autoUpdateMetadata: false`（任务 5.4 歌词走标题通道接管元数据），关闭了 SwiftAudioEx 唯一写 `elapsedPlaybackTime`/`playbackRate` 两键的自动通道；JS 侧 `Metadata.update` 只写标题/歌手/封面/时长，从不带时间键——锁屏面板没有锚点，iOS 26.6 上暂停回落 0、恢复从 0 外推。修法（仅 iOS 原生侧，`patches/react-native-track-player+2.1.2.patch`）：状态翻转（playing/paused/buffering）、seek、setRate 三处刷锚点；播放写实际速率与当前位置，暂停写速率 0 与暂停点（速率不归零面板暂停期间会继续外推），seek 写目标值（异步 seek 期间 `currentTime` 仍是旧位置）。Android 的 `autoUpdateMetadata` 在 ExoPlayback 侧（`MusicManager.java:124`），与本补丁零交集，Android 行为不变。判别面：`getNowPlayingInfo` 探针补 `rate` 回读；`playback` 自测加四组锚点断言（起播锚点+速率非零、暂停锚点=暂停点且速率归零、恢复锚点速率回填且不早于暂停点、JS 元数据全量提交后锚点存活）。判据：CI 锚点断言为回归面；面板渲染行为真机复测判读（模拟器可读键值但渲染不可外推）。**CI 判据达成（2026-09-07）**：run 34080700299（`f1063736`）五 job 全绿，`playback` 锚点四组断言全过且带数值——起播锚点 `elapsed=0.027/rate=1`、暂停锚点 `elapsed=0.9869`（与 `pausedPos2` 逐位相等）`rate=0`、恢复锚点 `elapsed=1.0226/rate=1`（不早于暂停点）、标题/歌词通道写入后锚点存活（`nowPlaying.elapsed=1.0226`）。Swift 补丁经 `ios-build` 编译、`patch-package` 本地往返双重验证。**真机判据达成（2026-09-07）**：用户复测确认「锁屏的时候暂停播放播放进度问题已经修复」。任务关闭
- [X] 9.12 横屏弹窗被强制转回竖屏（真机 2026-09-07：自定义源管理、排行榜选音源两处横屏下弹窗一呈现即被转回竖屏，无法操作——案例 19）：应用所有弹窗经 `common/Modal.tsx`（RN `Modal` 包装，`Dialog`/`Menu`/`DorpDownPanel`/`ChoosePath` 同一收口），未传 `supportedOrientations`；RN 0.73 原生侧 `RCTModalHostView` `supportedOrientationsMask` 对该缺省在 iPhone 上返回竖屏独占掩码，横屏呈现弹窗系统当场转竖屏。修法：`common/Modal` 显式传 `supportedOrientations=[portrait, landscape-left, landscape-right]`（与 Info.plist 三口径对齐）。Android 侧 `ReactModalHostManager.setSupportedOrientations` 为空实现，行为不变。判别面：`landscape` 用例横屏段经同一收口呈现真弹窗，原生探针 `modalOrientationProbe` 回读「呈现成功（`RCTModalHostViewController`）+ 方向掩码含横屏位 + 场景仍横屏」——旧实现三条全判负。**判据双向达成（2026-09-07）**：① run 34094071327（`58735a8`）冒烟 35/35 全绿，`landscape` 弹窗段带数值通过——呈现类名 `RCTModalHostViewController`、掩码 26（竖屏 2 + 横屏 24）、场景 `landscape-right` 未回转（首跑 run 34091023873 因探针 NSNumber 过桥 0/1 被严格布尔比较误判失败，判据写法修正见 `58735a8`，该构建数据已全对）；② 真机（iPhone 17 Pro / iOS 26.6）复测确认横屏弹窗问题解决（用户 2026-09-07 判读，依据 34091023873 构建）
- [X] 9.14 「文件」App 直开文档报文件不存在（真机 2026-09-07：「文件」App 点开 `.lxmc` 能唤起洛雪但弹「调用失败：文件不存在」——案例 20）：根因在 iOS 文档投递模型：外部打开递来的 `file://` URL 指向沙箱外（其他应用容器 / iCloud），安全作用域访问授权只在 `application:openURL:` 回调内可靠可用，JS 收到 Linking 事件再走 `stat` 时授权已失效、读不到原路径。此前任务 6.4 判据误记双路达成已更正：方案 A 导出落应用自身 Documents（沙箱内直读）、方案 B 成功走的是应用内 `selectFile` 选择器链路（任务 6.5/9.4），均非外部直开。**第一轮修复 `719d64f`（run 34113701768 五 job 全绿）真机复测切出两个独立残余（2026-09-07 晚）**：① 冷启动（进程未启动）仍报「调用失败 文件不存在」——进程未启动时 URL 不走 `openURL` 回调，而由 `RCTLinkingManager.getInitialURL` 从 `launchOptions` 直取，暂存没跑到；② 热启动错误形态变为「导入失败」（暂存与确认弹窗均已生效，错误形态变化本身即证据）——坏在解压导入段：`RNFS.stat` 返回的 `path` 字段原样回显输入的 `file://...`，`GzipModule` 原生侧按纯路径读文件带前缀即读失败。**第二轮修法两件**：① `AppDelegate.didFinishLaunchingWithOptions` 内经 `lx_stagedLaunchOptions` 改写 `launchOptions` 里的 `UIApplicationLaunchOptionsURLKey` 为暂存 URL，复用同一 `lx_stagedFileURL`；② `fs.ios.ts` `stat` 返回的 `path` 经 `stripFileScheme` 剥 `file://` 前缀（RNFS 内部 readFile 自行剥前缀不受影响，唯 `stat` 返回字段回显输入；调用点六个，仅深链链消费 `.path`，其余只读 name/size/canRead，剥前缀无破坏）。CI 判别面试过不可行：模拟器不强制沙箱隔离，外部路径照样可读、造不出失败形态；回归面由既有 `deeplink` 用例承担。判据：CI 五 job 全绿（`deeplink` 用例 `file://` 探针链路不回归）；真机复测冷热两种启动下「文件」App 直开 `.lxmc` 均唤起→确认弹窗→导入成功（同任务 9.11 判据结构，模拟器不可外推）。**第二轮真机结果（2026-09-07）**：热启动「导入成功了」——暂存 + 前缀剥除整条链路打通；冷启动仍报「调用失败 文件不存在」，第二轮 `launchOptions` 补口未生效。**第三轮根因假说与修法**：第一轮暂存的「`isReadableFileAtPath` 为真即透传」快速路径不可靠——冷启动窗口内系统对外部安全作用域文件短暂放行，该判断为真，但 JS 数秒后执行时放行已失效、`stat` 仍读不到。判据改为沙箱包含性：路径在 `NSHomeDirectory()` 之内（共享文档、CI 探针）才透传，之外一律经安全作用域拷贝。另加真机归因日志 `lx_logOpen`：每次处置（透传/暂存成功/暂存失败带 NSError/launchOptions 无 URL 带 keys 列表）追加一行到 `Documents/lx-open-log.txt`（`UIFileSharingEnabled` 使其出现在「文件」App 本应用共享目录），复测仍失败时可直接取出日志回传，区分「系统没投递」与「投递了但暂存失败」。方案 A 观察项归因（2026-09-07）：iOS 目录选择只落默认目录属平台事实——内置浏览器从 Documents 起步，系统级共享目录是其他应用沙箱，平台规则不允许跨沙箱写入，非缺陷、不修。判据：冷启动下真机「文件」App 直开 `.lxmc` 唤起→确认弹窗→导入成功；仍失败则取 `lx-open-log.txt` 归因。第三轮修复提交 `5d399f2`（run 34123980864 五 job 全绿）。**第三轮真机结果（2026-09-08）**：冷启动仍报「调用失败 文件不存在」——原生暂存路线对冷启动结构性不可救：暂存发生在 `didFinishLaunchingWithOptions`，但 in-place 模式的安全作用域授权在该时点尚未下发、拷贝必然失败；授权真正有效的 `openURL` 回调又晚于 JS 监听器注册窗口，事件直接丢失，两头落空。**第四轮换源头修法**：`Info.plist` 移除 `LSSupportsOpeningDocumentsInPlace`（任务 6.3 引入，无契约依赖）。去掉该键后系统对「打开方式」一律走标准拷贝投递：系统进程先把文件拷进应用 `Documents/Inbox` 再交付，递来的恒为沙箱内路径，冷热启动均不再依赖授权窗口（热启动经 `openURL`、冷启动经 `launchOptions`，与既有暂存逻辑的透传/暂存/日志三分支天然兼容，`lx-open-log.txt` 可回读实际投递形态）。方案 A「只能导出到默认目录」已归因为平台事实（见本条上文），不在本项。判据：冷启动下真机「文件」App 直开 `.lxmc` 唤起→确认弹窗→导入成功；若仍有残余，取 `lx-open-log.txt` 归因。第四轮修复提交 `d5c5a17`（run 34178735645 五 job 全绿）。**第四轮真机结果（2026-09-08）**：冷启动仍报「调用失败 文件不存在」，且引入回归——移除 `LSSupportsOpeningDocumentsInPlace` 后默认打开方式失效（点击 `.lxmc` 需手动选择打开方式）。第四轮撤回。**第五轮换路（2026-09-08）**：前四轮均在原生启动时机暂存，冷启动时 in-place 文档的安全作用域访问在 `didFinishLaunchingWithOptions` 时点结构性拿不到（授权下发晚于该时点、早于 JS 监听器注册的窗口不存在），路线本身不可行。改为 JS 时点暂存：① `Info.plist` 恢复 `LSSupportsOpeningDocumentsInPlace`（撤销第四轮回归）；② `UtilsModule` 新增 `importOpenedFile` 原生方法——`NSHomeDirectory()` 之内原样透传零拷贝，之外先裸拷贝（物化已完成形态）→ 作用域拷贝 → `NSFileCoordinator` 协调读取，三档递进，最多 5 次重试（0.4s 间隔覆盖 FileProvider 物化延迟），每步写 `Documents/lx-open-log.txt`，reject 带逐步诊断进错误弹窗；③ `fs.ios.ts`/`fs.ts` 新增 `importOpenedFile` 导出（Android 恒等透传），`fs_exports` 自测期望清单同步；④ 深链 `handleFileAction` 在 `stat` 前先经 `importOpenedFile` 暂存。判据：冷热两种启动下真机「文件」App 直开 `.lxmc` 均唤起→确认弹窗→导入成功；失败时错误弹窗自带逐步诊断 + `lx-open-log.txt` 可取出。第五轮修复提交 `ec19453`（run 34184258556 因协调器块内错误指针取址编译失败而红，`1257c8a` 修复，run 34187487146 五 job 全绿）。**第五轮真机结果（2026-09-08）**：冷启动报错弹窗自带完整诊断——`scoped=no` ×5 + `copyErr=The file ... couldn't be opened because there is no such file`。诊断面按设计发挥作用，根因钉死：① 安全作用域授权窗口只在 `openURL` 回调内，JS 运行时（无论启动阶段还是深链处理时点）`startAccessingSecurityScopedResource` 恒返回 NO；② 「文件」App 的 FileProvider 项冷启动时未物化，裸拷贝与协调器在作用域关闭后都读不到。结论：**暂存只能发生在 `openURL` 回调窗口内**——热启动导入成功（第二轮实证）即因该窗口有效；冷启动时 `openURL` 在启动完成后仍会被系统调用（投递初始 URL），产物此前没有传给 JS。**第六轮修法**：① `lx_stagedFileURL`（`didFinishLaunching`/`openURL` 共用）补 `NSFileCoordinator` 档——此前只有裸拷贝，缺触发 FileProvider 物化的通道（提交 `74ded2b`）；② `openURL` 暂存成功时经 `NSUserDefaults` 登记产物（`lxStagedFilePath`/`lxStagedFileSourceURL`），`importOpenedFile` 各拍先按源 URL 领取登记产物、命中即返回，产物晚于 JS 启动才登记也随拍轮询命中（提交 `ba68cb8`）。判据：冷启动真机「文件」App 直开 `.lxmc` 唤起→确认弹窗→导入成功；日志可见 `picked staged artifact`。第六轮修复提交 `74ded2b` + `ba68cb8`（run 34194737967 五 job 全绿）。**判据双向达成（2026-09-08）**：真机（iPhone 17 Pro / iOS 26.6）冷启动导入成功（用户确认），热启动此前已通（第二轮实证）。任务关闭
- [X] 9.15 本地网络权限声明缺失（任务 6.7 前置排查 2026-09-08）：数据同步对局域网内桌面端同步服务发起 TCP 连接（`/hello` 版本握手 + `/id` + WebSocket），iOS 14+ 对本地网络出站连接要求 `NSLocalNetworkUsageDescription`；缺失时系统不弹授权提示、连接静默失败且错误无文本可归因，6.7 首次连接大概率撞上。已确认 `Info.plist` 无该键（`rg` 实查）。修法：`Info.plist` 补 `NSLocalNetworkUsageDescription`（用途文案说明局域网同步服务连接）；`specs/ios-distribution` 补「本地网络权限声明」契约。注：与任务 9.7 移除的 `NSAllowsLocalNetworking` 无关——那是 ATS 例外字典键，这是运行时权限文案键，两者作用面不同。判据：下一轮 CI 五 job 全绿（ios-build 编译验证）；6.7 真机首连弹出本地网络授权提示且授权后连接建立。**判据达成（2026-09-09）**：CI run 34204326388（`2afb4b9`）五 job 全绿（ios-build 编译验证）；真机 6.7 用户确认正常（2026-09-09）——连接建立、同步完成。任务关闭
- [X] 9.16 导出保存位置限于应用沙箱（真机 6.7 备份恢复实测反馈 2026-09-09：备份导出只能选沙箱目录，不能选「文件」App 其他共享位置；此前任务 6.4 把此现象定性为「平台事实不修」，定性不准——平台限制的是目录选择器，不是导出保存位置）：导出链路走 `ChoosePath` 内置目录浏览器（`dirOnly:true`），只能浏览并写入应用自身沙箱。iOS 标准做法是系统另存为面板（`UIDocumentPickerViewController` export 模式），用户任选保存位置（「文件」App 任意目录 / iCloud / 第三方存储提供者），系统执行拷贝，是唯一合规的「保存到任意位置」路径。修法：① 原生 `UtilsModule.exportFile`（export 模式，呈现复用任务 9.4 竞态安全管线，回调按 controller 身份与导入分流，取消 `resolve(null)`，呈现预算耗尽走 `reject`，源文件不存在显式报错）；② `fs.ios.ts`/`fs.ts` 补 `exportFile`（Android 显式抛错防误用）；③ 新增平台扩展 `exportPicker.ts`（基名，Android 恒抛错 + `systemExportPicker=false`）与 `exportPicker.ios.ts`（`saveViaSystemExportPicker` 打包 gzip 后经面板拷贝，暂存以目标文件名落盘使建议名可读，`finally` 清理）；④ 设置页全量导出与歌单单表导出两处入口接入，iOS 直开面板不经目录浏览器，用户取消不算失败。基名文件必须存在：tsc 不做平台扩展解析，基名缺失新增 `Cannot find module`（toast 基名缺失两条既有错误同款）。判据：本地门禁 `tsc` 恰好 21、`npm test` 4/4、lint 3e+1w；CI 五 job 全绿（`fs_exports` 清单含 `exportFile`，ios-build 编译验证原生改动）；真机导出能经另存面板落到「文件」App 沙箱外位置且产物可导入。**判据达成（2026-09-09）**：本地门禁 `tsc` 恰好 21、`npm test` 4/4、lint 3e+1w；CI run 34324207504（`6aa216f`）五 job 全绿（`fs_exports` 清单含 `exportFile`，ios-build 编译验证原生改动）；真机（iPhone 17 Pro / iOS 26.6）用户 2026-09-09 确认「真机实测 完美 解决了」——设置页备份导出与歌单单表导出均经系统另存为面板落「文件」App 任意位置。任务关闭
- [X] 9.17 返回桌面与退出应用按钮无效（真机 2026-09-09 反馈：设置中「显示返回桌面按钮」与「显示退出应用按钮」点击后没有达到真正的效果）：两个按钮链路各有独立死点——① 返回桌面：`Aside.tsx`/`DrawerNav.tsx` → `tools.ts` `exitApp()` → `BackHandler.exitApp()`，`BackHandler` 为 Android 专属，iOS 上静默空操作；② 退出应用：确认弹窗 → `core/common.ts` `exitApp()`（先销毁播放器）→ `UtilsModule.exitApp()`，原生侧为空桩（「iOS 不允许应用主动退出，桩化为空实现」），播放器销毁后应用卡在原地。修法（对齐社区通行做法 `react-native-exit-app`）：iOS 上两路径统一收敛为挂起到后台（`UIApplication.suspend`，`exit(0)` 会被系统当崩溃记录、Apple 明确反对；本项目只走内部分发，设计 D7，无审核风险）。改动三处：① 原生 `UtilsModule.exitApp` 空桩改 `lx_suspend`（IMP 直调绕开 `performSelector` 对非对象返回的 ARC 检查，主线程派发），新增 `backHome` 同实现；② `nativeModules/utils.ts` 补 `backHome` 导出（可选调用，Android 无此方法保无操作）；③ `tools.ts` `exitApp()` 平台分支——Android 保持 `BackHandler.exitApp()`，iOS 走 `backHome()`。判据：门禁 `tsc` 恰好 21、`npm test` 4/4、lint 3e+1w；CI 五 job 全绿（ios-build 编译验证原生改动）；真机两按钮点按后应用挂起到后台——退出应用路径挂起前无音频残留，返回桌面路径后台播放保持。**判据达成（2026-09-09）**：门禁与 CI 绿（提交 `e7b1e02`，run 34332353469 五 job 全绿）；真机（iPhone 17 Pro / iOS 26.6）用户确认返回桌面挂起到后台且后台播放继续；退出应用路径的音频停止判据经 9.18 一并修复后在 run 34341455441 构建复测达成。任务关闭
- [X] 9.18 退出应用后后台切歌播放（真机 2026-09-09 反馈：9.17 修复后「退出应用」变成返回桌面 + 后台切歌播放，音乐没停）：归因——`core/common.ts` `exitApp` 的 `destroyPlayer()` 调 `TrackPlayer.destroy()`，fork 的 `destroy()` 内部经 `player.stop()` 清队列，逐件发射的 `PlaybackTrackChanged` 事件形状与自然播完一致，被 `service.ts` 判为播完 → `playerEnded` → `playNext` → `handlePlay` 发现 `isInitialized=false` 重新 `setup` 并起播。`setStop`/`handlePlayMusic`/`initTrackInfo` 有守卫，`destroy` 没有。修法：`destroy()` 首个原生操作前置位队列手术守卫（与 `setStop` 同款，本地不释放——`stop()` 事件经桥异步投递可能晚于 `destroy()` resolve 才到，立即释放会漏接；由下一次队列手术的令牌释放兜住），并同步清 JS 轨道镜像（同 `setStop` 口径）。平台语义说明（用户问「为什么不走上滑退出」）：iOS 不允许应用主动终止进程（`exit(0)` 被记为崩溃，`terminateWithSuccess` 私有 API 已移除，上滑退出是用户手势触发系统 SIGKILL，应用无法模拟），所有 iOS 应用的「退出」按钮实为挂起到后台，本项目与社区做法（`react-native-exit-app`）一致；正确语义是退出前播放彻底停掉再挂起，本任务修的正是停不干净的问题。判据：门禁 `tsc` 恰好 21、`npm test` 4/4、lint 3e+1w；CI 五 job 全绿；真机「退出应用」确认后应用挂起且无音频残留、无后台切歌。**判据达成（2026-09-09）**：门禁 `tsc` 恰好 21、`npm test` 4/4、lint 3e+1w；CI run 34341455441（`f288302`）五 job 全绿；真机用户确认「退出应用后后台切歌播放」问题修复——销毁守卫阻断 destroy→playerEnded→playNext 重起播链路。任务关闭
- [X] 9.19 横屏锁屏解锁后列表仍两排（真机 2026-09-09 反馈：锁屏前横屏，解锁后系统已回竖屏但歌单列表保持横屏两排；排行榜此前经 9.13 修复已正常）：归因——`SizeView` 的 `handleLayout` 吃 `dimensionsChangedRef` 门闩，门闩只由 `Dimensions.addEventListener('change')` 打开；旋转发生在应用挂起期间时场景非 active，RN 不发 Dimensions change 事件（同 `evidence/landscape-inactive-scene.md` 实证机制），解锁后 `onLayout` 即使触发也被门闩丢弃，`windowSizeTools` 停在横屏尺寸，`useWindowSize` 订阅者（`getRowInfo` 排数）不重排。排行榜正常是因为 9.13 已改 `useWindowSize` 显式参数，但排数数据源仍是 `windowSizeTools`，数据源不更新同样停在两排——用户观察「排行榜正常」指 9.12 弹窗方向问题，与本项数据源问题无关。修法：`SizeView` 补 `AppState` change 监听，回 `active` 时重开门闩并主动测量视图，尺寸与记录不符即临拍重开门闩强制重走 `handleLayout`（原生读数 + 状态栏高度 + `setWindowSize` 完整同步），两拍延迟（300/1200ms）覆盖旋转重排晚于 active 的时序差；Android 同款监听无害（尺寸一致时不等式不成立，纯空操作）。判据：门禁全绿；CI 五 job 全绿；真机横屏锁屏→解锁后歌单列表随竖屏变单排，反向竖屏锁屏→横屏解锁变两排。**判据达成（2026-09-09）**：门禁全绿；CI run 34341455441（`f288302`）五 job 全绿；真机用户确认横屏锁屏→解锁后列表随竖屏重排问题修复。任务关闭
